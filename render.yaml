services:
  - type: web
    name: study-gate-system
    runtime: python
    buildCommand: echo "Static site"
    startCommand: python -m http.server $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
